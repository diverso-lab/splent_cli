FROM python:3.13

# ----------------------------------------------------
# Create non-root user splent (UID=1000, GID=1000)
# ----------------------------------------------------
RUN groupadd -g 1000 splent && \
    useradd -m -u 1000 -g 1000 -s /bin/bash splent && \
    mkdir -p /workspace && chown -R 1000:1000 /workspace

WORKDIR /workspace

# ----------------------------------------------------
# Copy source code
# ----------------------------------------------------
COPY splent_framework/ splent_framework/
COPY splent_cli/ splent_cli/

# ----------------------------------------------------
# System dependencies
# ----------------------------------------------------
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        mariadb-client \
        curl \
        ca-certificates \
        gnupg \
        lsb-release \
        passwd \
        util-linux && \
    rm -rf /var/lib/apt/lists/*

# ----------------------------------------------------
# PlantUML (Java + plantuml.jar for diagram export)
# ----------------------------------------------------
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        default-jre-headless \
        graphviz \
        fonts-dejavu \
        librsvg2-bin && \
    mkdir -p /opt/plantuml && \
    curl -fsSL -o /opt/plantuml/plantuml.jar \
        https://github.com/plantuml/plantuml/releases/download/v1.2024.8/plantuml-1.2024.8.jar && \
    printf '#!/bin/sh\njava -jar /opt/plantuml/plantuml.jar "$@"\n' > /usr/local/bin/plantuml && \
    chmod +x /usr/local/bin/plantuml && \
    rm -rf /var/lib/apt/lists/*

# ----------------------------------------------------
# Node.js
# ----------------------------------------------------
RUN curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && \
    apt-get update && \
    apt-get install -y --no-install-recommends nodejs && \
    npm install -g npm@11 npx && \
    rm -rf /var/lib/apt/lists/*

# ----------------------------------------------------
# Docker CLI + Compose plugin
# ----------------------------------------------------
RUN mkdir -p /etc/apt/keyrings && \
    curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg && \
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian $(lsb_release -cs) stable" \
        > /etc/apt/sources.list.d/docker.list && \
    apt-get update && \
    apt-get install -y --no-install-recommends docker-ce-cli docker-compose-plugin && \
    rm -rf /var/lib/apt/lists/*

# ----------------------------------------------------
# Python packages
# ----------------------------------------------------
RUN pip install --no-cache-dir --root-user-action=ignore -e ./splent_framework && \
    pip install --no-cache-dir --root-user-action=ignore -e ./splent_cli && \
    pip install --no-cache-dir --root-user-action=ignore -e "./splent_cli[dev,uvl]"

# ----------------------------------------------------
# Install CLI Node dependencies
# ----------------------------------------------------
WORKDIR /workspace/splent_cli
RUN npm install

# ----------------------------------------------------
# Cleanup Python cache
# ----------------------------------------------------
WORKDIR /workspace
RUN find . -type d -name "__pycache__" -exec rm -r {} + && \
    find . -type f -name "*.pyc" -exec rm -f {} +

# ----------------------------------------------------
# Custom shell prompt
# ----------------------------------------------------
RUN bash /workspace/splent_cli/scripts/setup_prompt.sh

# ----------------------------------------------------
# Entrypoint
# ----------------------------------------------------
COPY splent_cli/docker/entrypoints/cli-entrypoint.sh /usr/local/bin/cli-entrypoint.sh
RUN chmod +x /usr/local/bin/cli-entrypoint.sh

ENTRYPOINT ["/usr/local/bin/cli-entrypoint.sh"]